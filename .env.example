# Root env example for running all services locally
# Copy this to .env and fill sensitive values before running `npm run dev:all`

# API service
JWT_ACCESS_SECRET=change-me-access-secret-32-chars-min
JWT_REFRESH_SECRET=change-me-refresh-secret-32-chars-min
# Optional: override other API settings
# MONGODB_URI=

# AI service
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY

# Judge service (no required envs by default)
